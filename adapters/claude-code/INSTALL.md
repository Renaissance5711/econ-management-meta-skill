# Claude Code Adapter

Install or reference the repository root `SKILL.md`, then allow it to delegate to `skills/*/SKILL.md`. Run the `emm` CLI from the repository environment.

Do not weaken the root skill's human-verification, state-transition, profile-policy, or integrity-lock rules. Claude Code may propose artifacts but may not convert suggestions into verified research decisions.
