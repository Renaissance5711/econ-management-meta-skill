# Codex Adapter

Expose the repository root `SKILL.md` as the governing instruction and keep the stage skills under `skills/` available for bounded delegation. Execute commands through `uv run emm`.

Do not weaken the root skill's human-verification, state-transition, profile-policy, or integrity-lock rules. Codex must stop when the root skill returns `UNAVAILABLE_IN_VERSION`.
