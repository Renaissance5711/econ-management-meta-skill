# Golden Project Fixtures

These fixtures exercise the callable v0.1.0 governance core. The valid protocol can be locked and verified. The unsafe profile intentionally attempts to weaken the human effect-verification gate and must fail with `PROFILE_WEAKENS_CORE`.
